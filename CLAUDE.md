# Claude Code Project Notes

Use `AGENTS.md` as the shared project instruction file. The most important Claude Code-specific expectation is to keep work split into narrow, parallel-safe branches and avoid editing files owned by another active worker.

For plugin and MCP recommendations, see `planning/TOOLING_RECOMMENDATIONS.md`.
